#compdef <name>

_<name>() {
	local context state line
	typeset -A opt_args

	_arguments -C \
		'(-h --help)'{-h,--help}'[Show help message]' \
		'--version[Show version]'
		# TODO: Add more options and arguments
}

_<name> "$@"
